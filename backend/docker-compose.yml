version: "3"
services:
  app:
    container_name: selfassessment-backend
    restart: always
    image: app:selfassessment-backend
    build: .
    volumes:
      - ./data/assets:/usr/src/app/data/assets
      - ./data/configs:/usr/src/app/data/configs
    ports:
      - "8000:8000"
    links:
      - mongo
  mongo:
    container_name: mongo
    restart: always
    image: mongo
    volumes:
      - ./data/db:/data/db
    ports:
      - "27017:27017"
